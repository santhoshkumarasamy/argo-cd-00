global:
  domain: argocd.santhoshkumar.click

configs:
  params:
    server.insecure: true
    server.basehref: "/argocd"
    server.rootpath: "/argocd"

server:
  ingress:
    enabled: true
    ingressClassName: nginx
    annotations:
      nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
